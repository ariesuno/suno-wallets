# Prompt 1.8 — B3 Positions Fetch (v3) — **suno-wallets**

Implemente o **consumo de posições do investidor** na B3 usando o **B3OfficialClient** criado no 1.6. Este prompt **não persiste** dados (persistência de RAW será no 1.9). Aqui vamos **buscar, paginar e retornar** as posições para inspeção/preview e para uso interno pelos próximos prompts.

## 🎯 Objetivo
- Consumir os **endpoints de posições** (por tipo de ativo, começando por **equities**) da **API v3** da B3.
- Suportar **filtro por período** obrigatório (`referenceStartDate`, `referenceEndDate`) e **paginação** (`page`, iniciando em **1**).
- Validar obrigatórios: **CPF** (11 dígitos), **datas** `YYYY-MM-DD`, `page >= 1`.
- Expor **endpoint de preview** (sem persistência) e **serviço de aplicação** que encapsula a orquestração.

## 🔗 Contrato oficial (v3 — equities)
- **Posições (Equities) v3**:  
  `GET {UrlData}/position/v3/equities/investors/{documentNumber}?referenceStartDate=YYYY-MM-DD&referenceEndDate=YYYY-MM-DD&page=1`
- **Headers obrigatórios** em TODAS as chamadas:
  - `Authorization: Bearer {access_token}` (via B3OfficialClient)
  - `X-Investor-CPF: {cpf}`
  - `Content-Type: application/json`

> Observações: `referenceStartDate` e `referenceEndDate` são **obrigatórios** para v3. Tratar **400/401/403/429/5xx** conforme política do 1.6 (retry/backoff + jitter para 429 e 5xx).

## 🧱 Arquitetura (DDD)
Crie os artefatos a seguir, **nomeando em inglês** e **comentando em português**:

- `src/Application/B3/positions/positions_service.go`  
  Serviço de aplicação que coordena o fetch por tipo de ativo e páginas, usando o `B3OfficialClient` (1.6).
- `src/Api/Controllers/B3/positions_controller.go`  
  Controller com o endpoint **preview**:
  - `GET /b3/fetch/positions/preview?cpf=...&start=YYYY-MM-DD&end=YYYY-MM-DD&assetType=equity&page=1&fetchAllPages=true|false`
  - **Não persiste** nada. Somente retorna o payload consolidado da B3.
- `src/Shared/Validation/b3_input_validation.go`  
  (Reaproveitar) Helpers de validação: CPF (11 dígitos), datas `YYYY-MM-DD`, `page >= 1`.
- `src/Shared/Dto/b3_positions_dto.go`  
  DTOs de request/response padronizados para este endpoint.

## ⚙️ Requisitos de Implementação
1. **Uso do B3OfficialClient (1.6)**  
   - Todas as chamadas HTTP devem passar por `makeRequest()` (mTLS, OAuth2, retry/backoff, timeouts, logs e métricas).
2. **Paginação**  
   - Implementar iteração de páginas quando `fetchAllPages=true` até não haver mais resultados.
   - Sempre validar que `page` inicial é **>= 1**.
3. **Tipos de Ativo (extensível)**  
   - Encapsular por `assetType` (ex.: `equity`, `fii`, `bdr`, etc.).  
   - **Implemente `equity` agora** conforme contrato v3. Os demais tipos devem possuir **interface** para expansão e podem lançar `NotImplemented` até os contratos serem confirmados.
4. **Validações obrigatórias**  
   - `cpf` (11 dígitos), `start`, `end` (`YYYY-MM-DD`), `page >= 1`.
   - `start` e `end` são **obrigatórios**; retornar erro **400** com mensagem clara quando inválidos.
5. **Observabilidade**  
   - Logs **estruturados (JSON)** com `tenantId`, `cpf_masked`, `endpoint`, `status`, `durationMs`, `retries`.
   - Métricas Prometheus: contadores de requisições/sucesso/erro por `assetType` e status; histograma de latência.
   - **Mascarar CPF** nos logs (nunca logar token).
6. **Erros & Resiliência**
   - Seguir política do 1.6: **retry/backoff + jitter para 429/5xx** e timeout por requisição.
   - Em caso de **401/403**, retornar erro com instrução de renovar credenciais.
7. **Swagger/Postman/Bruno**
   - Adicionar o endpoint `GET /b3/fetch/positions/preview` na tag `B3 Data` no **Swagger**.
   - Atualizar as **collections** do Postman e Bruno com exemplos completos de chamada (incluindo query params).
   - Incluir **exemplos cURL** em `docs/`.
8. **Sem persistência**
   - Não salvar nada no banco aqui. Persistência de RAW será feita no **1.9**.

## 🧪 Testes (obrigatório)
- **Unit** (`src/Tests/Unit/B3/positions_service_test.go`):
  - Validação de CPF/datas/página.
  - Montagem de URLs e querystring por período e `assetType`.
  - Paginação com `fetchAllPages=true` (simular múltiplas páginas).
  - Tratamento de erros HTTP 400/401/403/429/5xx.
- **Integration** (`src/Tests/Integration/B3/positions_preview_test.go`):
  - Smoke test do endpoint preview com envs reais (se disponíveis).  
  - **Nunca** criar dados fake sem autorização — se faltar dado real, **pausar e solicitar instruções** (conforme diretrizes).

## 📄 Documentação
- Atualizar **Swagger** com parâmetros e respostas (exemplos reais omitindo dados sensíveis).
- Atualizar **Postman/Bruno** com chamadas prontas para **preview**.
- Adicionar **README** curto em `docs/b3/positions.md` com exemplos cURL e checklist de validações.

## ✅ Entregáveis
- Controller e Service de posições (preview) implementados e testados.
- Validações, logs e métricas integradas.
- Documentação (Swagger + collections + docs) atualizada.
